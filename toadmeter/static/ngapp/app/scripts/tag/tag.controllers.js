/*global angular, console */
(function (ng) {
    'use strict';
    var mdl = ng.module('TagModule');

    // Too small to be separate controller
//    mdl.controller('Tag.ListCtrl', ['$q', '$scope', 'Tag',
//        function ($q, $scope, Tag) {
//            $scope.incomes = Tag.$collection({type: 'in'}).$refresh();
//            $scope.costs = Tag.$collection({type: 'out'}).$refresh();
//        }]);

}(angular));